# DESIGN.md

## 产品设计基线

本项目首版以 CLI、守护进程和协议接口为主，不提供 Web 管理台。

1. CLI 输出必须明确区分 Agent 身份、主机身份和 Tailscale 节点身份。
2. 危险操作（撤销、封禁、密钥轮换）必须展示目标指纹并要求显式确认或 `--yes`。
3. 默认输出适合人类阅读，同时为自动化命令提供 `--json`。
4. 错误信息不得泄露配对秘密、消息正文、附件内容或私钥路径。
